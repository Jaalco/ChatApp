#include <iostream>

using namespace std;

int main()
{
    // Prompt the user for server or client
    // Make the server a class, then run it as a thread
    // Same for client

    // UDP send bcast message
    // if server replies, then connect to it
    // else spawn the server

    cout << "Hello world!" << endl;
    return 0;
}
